<form method="post" action="{{ path('app_tarif_scolaire_delete', {'id': tarif_scolaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarif_scolaire.id) }}">
    <button class="btn">Delete</button>
</form>
