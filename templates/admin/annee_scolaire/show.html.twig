{% extends 'base.html.twig' %}

{% block title %}AnneeScolaire
{% endblock %}

{% block body %}
	<h1>AnneeScolaire</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ annee_scolaire.id }}</td>
			</tr>
			<tr>
				<th>Libelle</th>
				<td>{{ annee_scolaire.libelle }}</td>
			</tr>
			<tr>
				<th>Date_debut</th>
				<td>{{ annee_scolaire.dateDebut ? annee_scolaire.dateDebut|date('Y-m-d') : '' }}</td>
			</tr>
			<tr>
				<th>Date_fin</th>
				<td>{{ annee_scolaire.dateFin ? annee_scolaire.dateFin|date('Y-m-d') : '' }}</td>
			</tr>
			<tr>
				<th>Active</th>
				<td>{{ annee_scolaire.active ? 'Yes' : 'No' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_annee_scolaire_index') }}">back to list</a>

	<a href="{{ path('app_annee_scolaire_edit', {'id': annee_scolaire.id}) }}">edit</a>

	{{ include('admin/annee_scolaire/_delete_form.html.twig') }}
{% endblock %}
