{% extends 'admin_base.html.twig' %}

{% block title %}Nouveau Niveau - School Management System{% endblock %}

{% block page_title %}Ajouter un Niveau{% endblock %}

{% block body %}
    <div class="card bg-base-100 shadow">
        <div class="card-body">
            <h2 class="card-title">Informations du niveau</h2>
            
            {{ include('admin/niveau/_form.html.twig') }}
            
            <div class="card-actions justify-end mt-6">
                <a href="{{ path('app_niveau_index') }}" class="btn btn-ghost">Annuler</a>
                <button type="submit" class="btn btn-primary" form="niveau_form">Créer le niveau</button>
            </div>
        </div>
    </div>
{% endblock %}
